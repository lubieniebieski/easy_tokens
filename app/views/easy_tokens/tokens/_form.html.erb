<%= form_for @token, html: { class: "form" } do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>

  <div class="button-group">
    <%= f.submit 'Save', class: "button button--success" %>
    <%= link_to 'Back', tokens_path, class: "button" %>
  </div>
<% end %>
